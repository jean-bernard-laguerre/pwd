<?php

    require_once 'vendor/autoload.php';

    for($i = 0; $i < 10; $i++) {
        /* $product = new App\Electronic();
        $product->setName('Product ' . $i);
        $product->setPrice(rand(10, 100));
        $product->setPhoto(['photo' . $i]);
        $product->setQuantity(rand(1, 100));
        $product->setDescription('Description ' . $i);
        $product->setCreatedAt(new DateTime());
        $product->setUpdatedAt(new DateTime());
        $product->setIdCategory(rand(1, 5));
        $product->setBrand('Brand ' . $i);
        $product->setWarrantyFee(rand(1, 10));
        $product->save(); */

        /* $product = new App\Clothing();   
        $product->setName('Product ' . $i);
        $product->setPrice(rand(10, 100));
        $product->setPhoto(['photo' . $i]);
        $product->setQuantity(rand(1, 100));
        $product->setDescription('Description ' . $i);
        $product->setCreatedAt(new DateTime());
        $product->setUpdatedAt(new DateTime());
        $product->setIdCategory(rand(1, 5));
        $product->setSize('Size ' . $i);
        $product->setColor('Color ' . $i);
        $product->setMaterialFee(rand(1, 10));
        $product->setType('Type ' . $i);
        $product->save(); */
    }


